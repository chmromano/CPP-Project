#include "main.h"

int main() {

    Library library;


/*    std::ifstream input_file("library.txt", std::ios::in);

    Library library;

    input_file >> library;

    library.print_all();

    input_file.close();*/

    return 0;
}
